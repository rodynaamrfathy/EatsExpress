summary: "Create or update a cart"
description: "Creates a new cart or updates an existing one."
parameters:
  - in: "body"
    name: "body"
    description: "Cart object that needs to be added or updated."
    required: true
    schema:
      $ref: "#/definitions/Cart"
responses:
  201:
    description: "Cart created or updated successfully."
    schema:
      $ref: "#/definitions/Cart"
